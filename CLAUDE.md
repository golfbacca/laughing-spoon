# 三原誠司さんの副業リポジトリ

生成AIを使ったプロダクト作成・販売を、複数チャネルで並行して進めている。
このファイルは**全案件に共通する前提**。案件ごとの詳細は各ディレクトリを見ること。

## 相手のこと（最初に読む）

- **英語が苦手。** 英語のコピーは必ず日本語訳と語感の解説を添える。
  「なぜこの言い回しが効くか」まで書く
- **PC操作の指示は1クリック単位で。** ボタンの色・位置・文言まで書く
  （本人の言葉:「バカでも分かるように細分化して」）。
  URLは bash コードブロックに入れると実行ボタンが付いて親切
- PowerPoint / Outlook のマクロは得意。**その領域は説明を省いてよい**
- **パスワード入力・支払い・OAuth許可・金融口座の操作は必ず本人。** こちらは代行しない。
  公開・投稿・設定変更も、実行前に必ず確認を取って止まる
- **デザインの違和感を鋭く指摘してくる。** 「なんとなく変」ではなく**実測で白黒つける**と喜ばれる。
  実績として、本人が違和感を訴えた4件は4件とも本当に破綻していた。**軽く扱わないこと**
- 二度手間をかけたときは**原因を明示して簡潔に謝る**。長い自己批判は求められていない

## 作業スタイル

- **実測してから言う。** 「たぶん大丈夫」ではなく数値で示す
- **生成はこちら、操作は本人。** ブラウザ自動操作が使えない前提で段取りする
- 決定事項と成果物は**必ずこのリポジトリに書き出す。** チャット履歴の中にしか無い情報は、
  次のセッションで失われる（実際に失われた。`garden_flag/docs/03_過去資産の所在.md` 参照）

## 環境の制約

実行環境が2種類あり、**使えるものが違う**。まず自分がどちらにいるか確認すること。

### ローカル（本人のWindows PC上のClaude Code）

| 手段 | 状態 |
|---|---|
| アプリ内ブラウザ | printify.com でクラッシュ。**使わない** |
| Chrome拡張 | printify.com / etsy.com とも「Navigation to this domain is not allowed」 |
| Chrome拡張で gemini.google.com | **使える**（画像生成で実績あり） |
| PowerShell で Etsy を fetch | DataDome が 403。ただし `i.etsystatic.com` のCDN画像は取得可 |

### クラウド（claude.ai/code のリモートセッション）

| 手段 | 状態 |
|---|---|
| printify.com | **egress proxy でブロック**。到達不可 |
| etsy.com | WebFetch が 403 |
| 本人のローカルディスク（`C:\...`） | **一切見えない**。ファイルは添付・Drive・リポジトリ経由で渡してもらう |
| Google Drive / Gmail | **読める**（MCP経由）。過去資料の発掘に有効 |
| Driveの**画像の実データ** | `download_file_content` はbase64で返る。**8MBのPNG1枚で膨大なトークンを消費**するので実用外。`read_file_content` は画像に対して空を返す |
| `drive.google.com` へ直接curl | **プロキシがブロック**（printify.com と同じ） |
| **チャットに添付された画像** | ✅ **`/tmp/claude-0/<...>/images/*.webp` にファイルとして保存され、Pillowで直接処理できる。画像を渡す最良のルート** |
| GitHub | 読み書き可（MCP経由） |

→ どちらの環境でも、**EtsyとPrintifyの画面操作は本人にやってもらう**のが基本。
こちらは1クリック単位の手順書を出す側に回る。

## 案件一覧

| ディレクトリ | 案件 | 状態 |
|---|---|---|
| `golf_towel/` | Etsy「ONE PURE STRIKE」ゴルフタオル（Printify製造） | 9商品公開中・**売上0** |
| `garden_flag/` | Etsy ガーデンフラッグ（米国向け・クリスマス／ウィンター） | 企画中 |
| `stickers/` `assets_charA/` `docs/` | LINEスタンプ（エモ文字・シリーズA/B） | 審査〜販売開始 |

**他ブランチに別案件がある**（KDP絵本、Amazonぬりえ、SUZURIスタンプ等）。
`git branch -r` で確認すること。

## いま最優先の論点（2026-09-16）

ゴルフタオル9商品は、**Etsy検索からの流入が全期間ゼロ**。
売れない原因は商品側ではなく、検索結果に出ていないこと。
根拠と対策は `garden_flag/docs/04_ゴルフタオル実績の診断.md` にまとめてある。

**新しい商品を作る前に、この問題を片付けるかどうかを本人と決めること。**
同じ売り方で商品を足しても、同じゼロになる。
